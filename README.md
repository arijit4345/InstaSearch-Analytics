# InstaSearch-Analytics

Develop a web application that assists a logistics company in planning delivery operations across multiple locations. The application should accept a .txt dataset file containing pairs of locations and the distance between them. Users should be able to explore the transportation network, determine efficient routes between selected locations, and generate an optimized network connecting all available locations. The system should provide a user-friendly interface for visualizing the network and displaying the computed results.
